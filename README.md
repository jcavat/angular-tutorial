# Angular tutorial

Configure and run the server:
```
cd server
npm install
npm start
```

## Step 1 mission:

### Install angular cli 
* `npm install -g @angular/cli`

### Create a new angular project
* ...
